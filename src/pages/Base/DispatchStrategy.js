import React, { Component } from 'react'
import PropTypes from 'prop-types'

export default class DispatchStrategy extends Component {
  static propTypes = {
  }

  render() {
    return (
      <div>
        Dispatch Strategy
      </div>
    )
  }
}
